# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# LLM
google-genai>=1.0.0

# Lightweight retrieval (production / Render)
rank-bm25>=0.2.2

# Data processing
pandas>=2.2.0
numpy>=1.26.0
beautifulsoup4>=4.12.0

# HTTP & logging
httpx>=0.27.0
loguru>=0.7.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Dev tools
black>=24.10.0
ruff>=0.8.0
isort>=5.13.0

# Optional: embedding retrieval (local dev only — not installed on Render)
# pip install -r requirements-embeddings.txt
