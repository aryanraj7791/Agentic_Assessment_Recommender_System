services:
  api:
    build: .
    ports:
      - "7860:7860"
    environment:
      PORT: "7860"
      LLM_PROVIDER: mock
      RETRIEVAL_MODE: keyword
      LAZY_INIT: "false"
    env_file:
      - .env
